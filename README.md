# react-17-0-1-template

Command

    "start": "webpack serve",
    "build": "webpack --mode production",
    "format": "prettier --write '{*.js, src/**/*.{js|jsx}}'",
    "lint": "eslint src/ --fix"
    
